import express from "express";

const router = express.Router()

// полный путь - /api/planning/events/comments/
// router.get('/comments/:taskId', handlers.getEventCommentList)
// router.post('/comments/add', handlers.addEventComment)
// router.post('/comments/remove', handlers.removeEventComment)

export const CommentsRouter = router